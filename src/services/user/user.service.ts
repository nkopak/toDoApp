// import { db } from '../../data/db';
// import { IUser } from '../../models';

// class UserService {
//   createUser(userObject: Required<IUser>) {
//     const user = db('users').insert(userObject);

//     return user;
//   }
// }

// export const userService = new UserService();
